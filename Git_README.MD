One Time Setup
-----
git config --global user.name "Name"
git config --global user.email "Email"
git config --global core.editor "vim"


Daily Workflow
--------------
git clone <URL>
git status
git add <filename>
git commit
